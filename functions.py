# This is an advanced intro to functions in Python.

def build_character(name, age, occupation):
    """Build a character with the given name, age, and occupation."""
    character = {
        'name': name,
        'age': age,
        'occupation': occupation
    }
    return character